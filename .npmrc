# Copy file: dependencies into node_modules instead of symlinking them.
# family-auth lives outside this folder, and the deploy ships node_modules as
# files, so a symlink would arrive broken on the server. A symlinked package
# also resolves its peer dependencies (React) from its own folder, not ours.
install-links=true
